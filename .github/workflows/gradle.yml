name: Action sistema bancário

on:
  push:
    branches: [ develop, feature/unit_tests ]
  pull_request:
    branches: [ develop, feature/unit_tests ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
      
      - uses: gradle/gradle-build-action@v2
        with:
          arguments: test

      #- name: Resolve project dependencies
        #run: ./gradlew --refresh-dependencies

      #- name: Compile
        #run:  ./gradlew bootJar

      #- name: Run unit tests
        #run: ./gradlew test

